namespace PhaseShift.UI;
internal static class AppConstants
{
    public static string AppName => "PhaseShift"; // must also be changed in csproj
    public static string AppVersion => "0.3.1"; // must also be changed in csproj
}
